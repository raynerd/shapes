{
  "name": "atm",
  "version": "1.0.0",
  "description": "An awesome ATM machine for a not existing bank",
  "main": "apps.js",
  "scripts": {
    "test": "mocha --require babel-core/register",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "mocha": "^5.0.4",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "express": "^4.16.2"
  }
}
